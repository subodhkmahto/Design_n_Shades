
            <% if (locals.lastVisit) { %>
                <div class="alert alert-warning" role="alert">
                    <p>Your last visit was on: <%= locals.lastVisit %></p>
                    A simple warning alert—check it out!
                </div>
            <% } %>


        <table class="table table-bordered  header">
            <thead class="thead-dark">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Price</th>
                    <th>Image</th>
                    <th>Action</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% products.forEach(product => { %>
                <tr>
                    <td><%= product.id %></td>
                    <td><%= product.name %></td>
                    <td><%= product.desc %></td>
                    <td><%= product.price %></td>
                    <td><img src="<%= product.imageUrl %>" alt="<%= product.name %> Image" class="img-fluid" /></td>
                    <td><a href="/product/update-product/<%=product.id%>" class="btn btn-primary">Update</a></td>
                    <td>
                        <button class="btn btn-primary" onclick="deleteProduct('<%= product.id %>')">Delete</button>
                    </td>
                                        <!-- <td><a href="/delete-product/<%=product.id%>" class="btn btn-primary">Delete</a></td> -->
                   <!-- <td> <form method="post" action="/delete-product/<%= product.id %>" style="display:inline;">
                        <button type="submit" class="btn btn-primary">Delete</button>
                    </form></td> -->
                </tr>
                <% }) %>
            </tbody>
        </table>
   